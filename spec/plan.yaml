version: 0.1
architecture:
  components:
    ingest: "file upload + event receiver"
    storage: "Postgres (future) + object store (local for MVP)"
    metrics: "pandas/in-memory (MVP)"
    insights: "Rule-based engine (Python)"
    api: "FastAPI (Dockerized)"
    ui: "Next.js (stub later)"
privacy_security:
  pii: "no raw PII; hashed user identifiers"
  residency: "configurable storage and TTL"
observability:
  logging: "structured JSON"
  tracing: "OpenTelemetry (future)"
deploy:
  local: "docker compose"
  prod: "containerized, on-prem friendly"
